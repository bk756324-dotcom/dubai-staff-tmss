# PostgreSQL Production Database Connection Strings (Neon, Supabase, Cloud SQL, etc.)
DATABASE_URL=
DATABASE_DIRECT_URL=

# JWT Secret Key for TMS Session Tokens (set a strong, unique secret in production)
JWT_SECRET=

# Runtime Environment
NODE_ENV=production
